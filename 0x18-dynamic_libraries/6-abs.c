#include "main.h"

/**
 * _abs - Computes the value of an integer
 *@n: the value of int to be computed
 *
 * Return: value of the integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}

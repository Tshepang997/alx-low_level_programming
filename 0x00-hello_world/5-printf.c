#include <stdio.h>

/**
 * main - print out the printf function
 *
 * Return: always 0 when its successful
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
